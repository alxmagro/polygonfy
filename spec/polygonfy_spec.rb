require "spec_helper"

describe Polygonfy do
  it "has a version number" do
    expect(Polygonfy::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
